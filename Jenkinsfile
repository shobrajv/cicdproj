pipeline {
    // agent { docker { image 'python:3.7-alpine' } }
    // agent { docker { image 'alpine' } }
    agent any
    stages {
        stage('build') {
            steps {
                bat 'echo hello'
            }
        }
    }
}